    // Hoisted globals to ensure zero TDZ (Temporal Dead Zone) across on-air update calls
    var currentLogoShape = 'squircle';
    var currentRatingShape = 'squircle';
    var currentShowTag = 'none';
    var currentMasterPresetId = 'default';

